#summary : Linux: install "libglut.so.3".

Matlab produces the following error when trying to run a DPX experiment:

"libglut.so.3: cannot open shared object file: No such file or directory"
Solution

Install libglut.so.3 using the following in a Linux-terminal and restart Matlab
{{{
sudo apt-get install freeglut3
}}}